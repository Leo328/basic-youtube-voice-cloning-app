services:
  # Backend API service
  - type: web
    name: backend
    runtime: python3.9
    buildCommand: cd backend && pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT --workers 1 --worker-class uvicorn.workers.UvicornWorker backend.main:app
    envVars:
      - key: ELEVENLABS_API_KEY
        sync: false
      - key: PYTHON_VERSION
        value: 3.9.0
      - key: USE_STEALTH_MODE
        value: true
      - key: FRONTEND_URL
        fromService:
          name: voice-clone-frontend
          type: web
          property: url

  # Frontend web service
  - type: web
    name: voice-clone-frontend
    env: node
    buildCommand: cd frontend && npm install && npm run build
    startCommand: cd frontend && npm run preview
    envVars:
      - key: VITE_API_BASE_URL
        fromService:
          name: backend
          type: web
          property: url 